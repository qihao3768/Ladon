plugins {
    id 'com.android.library'
    id 'org.jetbrains.kotlin.android'
}

apply from:'../global.gradle'

android {
    resourcePrefix Prefix.common
}

dependencies {
    api project(Modules.lib_base)
}